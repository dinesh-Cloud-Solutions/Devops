{
  "name": "frontend-workflow-test",
  "version": "1.0.0",
  "scripts": {
    "test": "echo 'Running frontend tests...' && exit 0",
    "build": "echo 'Building frontend assets...' && mkdir -p dist && cp index.html dist/"
  }
}